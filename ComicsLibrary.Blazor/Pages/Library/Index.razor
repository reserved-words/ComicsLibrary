@page "/library/{ShelfId}"
@page "/library"
@inherits Pages.Library.IndexBase

@if (Items == null)
{
    <Loading />
}
else
{
    <Library Items="@Items" Shelf="@Shelf" ShelfName="@ShelfName" Actions="@Actions" />
}